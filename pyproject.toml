[project]
name = "apidump"
version = "0.1.0"
description = "Generate deterministic Markdown API references for Python packages"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project.scripts]
apidump = "apidump.cli:main"

[dependency-groups]
dev = [
    "pynapple>=0.11.0",
    "pytest>=8.0.0",
]
